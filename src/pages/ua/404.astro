---
import { t, changeLanguage } from "i18next";
import { localizePath } from "astro-i18next";
import Layout from "../../layouts/Layout.astro";

changeLanguage("ua");

/*const { pathname } = Astro.url;
if (pathname.slice(0, 3) === "/ua") {
    return Astro.redirect("/ua/404");
}*/
---

<Layout isHeader={false} title="Page 404">
    <div class="flex flex-col justify-center items-center w-full h-screen font-sans bg-blue-light">
        <h1 class="font-sans text-5xl font-medium">{t('general.page404')} - 404</h1>
        <h2 class="font-sans text-2xl text-gray-dark font-medium pt-5">{t('general.desc404')}</h2>
        <p class="text-3xl font-medium pt-8">{'=>'} <a class="text-gray-dark uppercase hover:text-yellow hover:underline transition" href={localizePath("/")}>{t('menu.home')}</a> {'<='}</p>
    </div>
</Layout>

